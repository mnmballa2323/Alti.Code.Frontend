# Alti.Code.Studio Memory

> *Persistent context and architectural notes for AI Agents.*

## 🏗️ Architectural Patterns
-   **Backend**: Uses a module-based architecture (`src/app/modules/`). Each module contains its own Controller, Service, Route, Model, and Interface.
-   **Frontend**: Next.js 15 App Router. State management via Redux Toolkit (`store/`). UI components are largely custom or wrapped Shadcn/HeroUI.
-   **YAML DSL Agents**: New specialist agents can be defined as `.agent.yaml` files in `src/app/modules/agents/definitions/`. The `yaml_agent_loader.js` dynamically creates `BaseSpecialistAgent` subclasses and supports hot-reload via `fs.watch` — no server restart required.
-   **Marketplace Registry**: Installed agents are tracked in `src/app/modules/agents/plugins/registry.json` (primary) and `MarketplacePlugin` Mongoose collection (secondary). If Mongo is unavailable, the file-based registry is used as fallback.
-   **Collaborative Developer Swarm**: A stateful 6-agent multi-agent pipeline (`SwarmEngine` inside `custom_swarm_orchestrator.js`) executing autonomous software developer sprints: `SwarmArchitect` -> `SwarmHermesDebate` (adversarial debate chamber using `hermes_debate_chamber.service.js`) -> `SwarmTddCoder` -> `SwarmQaTester` -> `SwarmSecurityAuditor` -> `SwarmDevOpsEngineer`.
-   **Knowledge Catalog & OKF Integration**: Integrates Google Cloud Dataplex (Knowledge Catalog) with Open Knowledge Format (OKF) parsing and synchronization (`src/app/modules/knowledgeCatalog/`). It validates Markdown metadata concepts, executes pushes/pulls to Dataplex, runs autonomous enrichment, and grounds conversational query results with precise citations. Additionally, it integrates bidirectionally with the Developer Swarm (`SwarmArchitect` queries catalog to reuse schemas/APIs, `SwarmDevOps` registers sprint outputs as OKF files) and powers active RAG pipelines (checking commit diffs against playbooks during code reviews, embedding incident playbooks in runbooks, and self-healing catalogs upon commit merges).
-   **5 Core AI-Native Engineering Pillars & Astro Flue Integration**: Employs Astro's `@flue/runtime` local sandbox execution framework to run untrusted code snippets. Features an AST traversal navigator (`ast_graph_navigator.js`) for context token pruning, a multi-model adversarial Socratic debate chamber (`socratic_debate_chamber.js`) leveraging AWS Bedrock/GCP Vertex/Azure OpenAI Foundry models, a write-time policy compliance gate (`policyComplianceGate.js`) restricting secrets, copyleft licenses, and missing CSRF headers, and a background self-evolution daemon (`run_autopoietic_evolution_daemon.js`) to autonomously resolve technical debt and fix warnings.
-   **Neo4j AST Code-Graph & Git Blame Context Grounding**: Synchronizes classes, methods, and file dependency mappings directly to Neo4j database (`AstGraphNavigator.syncFileToNeo4j`). Grounding services utilize `git blame --porcelain` and `git show` (`GitContextGrounder.getLineBlameContext`) to fetch authorship and commit messages, augmenting Socratic debate chambers with structural and historical intent context. Workspace file modifications trigger websocket broadcasts via `socketService.broadcast` and async Neo4j re-indexing.
-   **Autopoietic Sentry Healing Loops & Codebase Memory MCP**: Polling daemon (`SentryHealingLoopService`) consumes production crash events, generates replication tests inside isolated Flue sandboxes, runs Socratic debates to resolve bugs, verifies patches in the sandbox, runs compliance scans, and exports git patches. DeusData's local-first Tree-sitter powered `codebase-memory-mcp` is deeply integrated inside MCP client presets and server configurations, enabling 3D repository visualization on port 9749.
-   **Air-Gapped Magnum deployment & Local Model Routing**: Bypasses public clouds and external network registry calls for vector embeddings, reasoning, and deployment when air-gapped (`AIR_GAPPED_MODE === 'true'`). Routes code-related tasks to a local `codestral` model and reasoning/Socratic critiques to `llama3` via Ollama. Text embeddings are cached in MongoDB or a JSON file-based fallback (`OfflineEmbeddingCache`). The OpenStack deployer (`deploy_openstack.sh`) preloads essential Kubernetes cluster system images into a private registry, overrides Magnum COE templates, and configures Helm to run locally in offline mode.
-   **v53.0.0 Platform Enhancements (Performance, Security, Compliance, & Diagnostics)**: Implemented debounced AST indexing queue for Neo4j (`AstGraphNavigator.syncFileToNeo4j`) with a configurable delay to prevent database write locks. Hardened the Astro Flue sandbox environment using `isolated-vm` to run untrusted code in resource-limited and context-free JS scopes, with a subprocess signal detection and automatic secure fallback to a standard Node `vm` context in unsupported environments (e.g. Apple Silicon segfaults). Hardwired outgoing payload credential scrubbing in the `AIProvider` gateway (`ai.provider.js`) to automatically redact API keys, Slack tokens, private keys, and DB passwords prior to calling external APIs. Integrated pre-reproduction compliance gates to verify Sentry event code snippets prior to executing them in the reproduction sandbox, and created a centralized `GET /api/v1/diagnostics/health` endpoint returning database, Neo4j, AI, disk, and memory status.
-   **v53.0.1 Platform Dependencies (MIT Packages & License Verification)**: Integrated `fast-glob` (for faster workspace traversal), `js-yaml` (for parsing specialist agent configs), and `magic-string` (for AST refactoring/patching) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.2 Platform Dependencies (Zip, Similarity, and Concurrency Packages)**: Integrated `adm-zip` (zero-dependency in-memory zip files), `fastest-levenshtein` (high-performance Levenshtein matching), and `p-limit` (promise concurrency control) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.3 Platform Dependencies (AST, Comment, and Concurrency Packages)**: Integrated `detect-indent` (precise code indentation layout detection), `p-map` (concurrency-limited promise mapping), and `strip-comments` (comment stripping utility for token pruning) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.4 Platform Dependencies (Lexer, Binary Filter, and ANSI Stripper Packages)**: Integrated `es-module-lexer` (fast JS/TS import/export analyzer), `is-binary-path` (checks paths for binary structures), and `strip-ansi` (removes ANSI escape codes from console logs) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.5 Platform Dependencies (Duration, Glob, and URL Validator Packages)**: Integrated `ms` (formats durations to readable strings), `is-glob` (wildcard string checker), and `is-url` (link string validator) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.6 Platform Dependencies (Comparison, IP, and Glob Matching Packages)**: Integrated `dir-compare` (recursively compares workspace directories), `is-ip` (IP string validator), and `picomatch` (high-performance glob matcher) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.7 Platform Dependencies (Archiver, Media types, and Priority Queue Packages)**: Integrated `archiver` (generates tarball/zip archive buffers for local snapshotting/transport), `mime-types` (media type extraction based on file extensions), and `p-queue` (priority and rate-limited queue execution for scheduling async agent tasks) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.8 Platform Dependencies (Resilient Retries, Bytes format, and LRU Cache Packages)**: Integrated `quick-lru` (high-performance Least Recently Used caching), `p-retry` (resilient execution with retries and exponential backoff), and `pretty-bytes` (formats bytes to readable strings) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.9 Platform Dependencies (Safe stringify, Deferred promises, and Async Timeout Packages)**: Integrated `fast-safe-stringify` (safe stringifier handling circular references), `p-defer` (lightweight deferred promises), and `p-timeout` (asynchronous timeout control utility) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.10 Platform Dependencies (Fast Memoize, Promise Any, and Promise Filter Packages)**: Integrated `fast-memoize` (high-performance caching of heavy CPU operations like AST mapping/parsing), `p-any` (parallel promise execution returning the first fulfilled value to reduce Socratic/LLM call latency), and `p-filter` (concurrent asynchronous list filtering for fast policy scans) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.11 Platform Dependencies (Promise Settle, Promise Props, and Promise Race Packages)**: Integrated `p-settle` (concurrently settles multiple promises with status indicators to orchestrate parallel swarm tasks), `p-props` (resolves keys of objects concurrently to gather structured AST/Git data), and `p-race` (safe competition routing between multiple LLM model results) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.12 Platform Dependencies (Promise Throttle, Promise Debounce, and Promise WaitFor Packages)**: Integrated `p-throttle` (rate-limits promise-returning execution rates to prevent model gateway or resource overloads), `p-debounce` (debounces asynchronous function calls to minimize redundant AST or database writes), and `p-wait-for` (asynchronously waits for targeted states or consensus criteria to resolve) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.13 Platform Dependencies (Promise Series, Promise Times, and Promise Progress Packages)**: Integrated `p-series` (executes promise-returning and async functions in a strict sequential order to avoid resource conflicts), `p-times` (runs promise-returning functions a specific number of times concurrently for multi-iteration agent loops), and `p-progress` (creates promises that report intermediate execution status for progress reporting in agent UI/dashboards) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.14 Platform Dependencies (Promise Event, Get Stream, and Is Stream Packages)**: Integrated `p-event` (promisifies event emitters to wait for process/socket exits or completions as async promises), `get-stream` (safely captures stdout/stderr streams into string variables for log ingestion), and `is-stream` (validates Node.js stream types for pipeline execution gates) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.15 Platform Dependencies (Promise Map Series, Promise Pipe, and Promise Catch If Packages)**: Integrated `p-map-series` (maps over promises serially to avoid parallel execution conflicts), `p-pipe` (composes promise-returning functions into reusable pipelines for clean agent step workflows), and `p-catch-if` (handles conditional promise recovery parameters to support selective error fallbacks) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.16 Platform Dependencies (Strip JSON Comments, Promise Lazy, and Promise Try Packages)**: Integrated `strip-json-comments` (strips comments from JSON strings), `p-lazy` (creates lazy promises that only execute when awaited), and `p-try` (wraps synchronous or asynchronous blocks into a promise chain) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.17 Platform Dependencies (Promise Reflect, Promise Locate, and Promise Some Packages)**: Integrated `p-reflect` (wraps promises to settle instead of throwing), `p-locate` (picks the first resolved promise satisfying a testing predicate), and `p-some` (threshold promise solver resolving when a specified count resolves) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.18 Platform Dependencies (Promise Reduce, Promise Each Series, and Promise Immediate Packages)**: Integrated `p-reduce` (sequentially reduces arrays using async promise accumulators), `p-each-series` (iterates async callbacks sequentially), and `p-immediate` (yields event-loop control immediately) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.19 Platform Dependencies (Promise Log, Promise Memoize, and Promise Min Delay Packages)**: Integrated `p-log` (logs promise values/errors asynchronously), `p-memoize` (caches promise resolutions), and `p-min-delay` (delays a promise-returning function by a minimum duration) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.20 Platform Dependencies (Promise Forever, Promise Is Promise, and Promise Waterfall Packages)**: Integrated `p-forever` (executes promise-returning functions repeatedly until a stop condition resolves), `p-is-promise` (type-guards object thenables to verify active Promise properties), and `p-waterfall` (composes waterfall promise chains where results flow sequentially as parameters to subsequent promises) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.21 Platform Dependencies (Promise Time, Promise Whilst & Promise Break Packages)**: Integrated `p-time` (measures promise execution duration), `p-whilst` (executes promise-returning functions repeatedly whilst a condition is true), and `p-break` (breaks out of active promise loops) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.22 Platform Dependencies (Promise Do Whilst, Promise All & Promise State Packages)**: Integrated `p-do-whilst` (executes promise-returning functions sequentially checking conditions after execution), `p-all` (resolves promise-returning functions concurrently), and `p-state` (inspects promise status returns) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.23 Platform Dependencies (Promise If, Promise Tap & Arrify Packages)**: Integrated `p-if` (conditional promise builder), `p-tap` (runs side-effects in promise chains), and `arrify` (converts values into arrays) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.24 Platform Dependencies (Array Union, Array Differ & Map Obj Packages)**: Integrated `array-union` (uniquifies arrays), `array-differ` (computes differences between arrays), and `map-obj` (maps object keys/values) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.25 Platform Dependencies (Utility Types, Execa & Globby Packages)**: Integrated `utility-types` (TypeScript utility types collection), `execa` (process execution utility), and `globby` (file globbing utility) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.26 Platform Dependencies (Strip BOM, Strip Indent & Indent String Packages)**: Integrated `strip-bom` (UTF-8 BOM remover), `strip-indent` (whitespace indentation remover), and `indent-string` (line indentation utility) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.27 Platform Dependencies (Camelcase, Decamelize & Dot Prop Packages)**: Integrated `camelcase` (string camel-casing utility), `decamelize` (decamelizes string formats), and `dot-prop` (nested object dot-path manager) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.28 Platform Dependencies (Plain Object Checker, Clean Stack & Aggregate Error Packages)**: Integrated `is-plain-obj` (checks if values are plain objects), `clean-stack` (strips system path traces), and `aggregate-error` (groups multiple errors) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.29 Platform Dependencies (Escape String Regexp, Resolve From & Import Fresh Packages)**: Integrated `escape-string-regexp` (escapes RegExp characters), `resolve-from` (resolves module path from directory), and `import-fresh` (imports module bypassing cache) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.30 Platform Dependencies (Parent Module, Path Exists & Find Up Packages)**: Integrated `parent-module` (gets parent module path), `path-exists` (checks path existence), and `find-up` (finds file/directory walking up) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.31 Platform Dependencies (Pkg Dir, Strip Outer & Trim Repeated Packages)**: Integrated `pkg-dir` (finds root of npm package), `strip-outer` (strips start/end substring), and `trim-repeated` (trims repeated substrings) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.32 Platform Dependencies (Escape Goat, Caller Path & Caller Callsite Packages)**: Integrated `escape-goat` (HTML entities escaping), `caller-path` (gets caller function path), and `caller-callsite` (gets caller function callsite) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.33 Platform Dependencies (Dynamic Packages)**: Integrated `path-type` (Check if a path is a file, directory, or symlink) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.34 Platform Dependencies (Dynamic Packages)**: Integrated `is-fn` (Check if a value is a function), `is-regexp` (Check if a value is a regular expression), `filenamify` (Convert a string to a valid safe filename), `mimic-fn` (Make a function mimic another one), `onetime` (Ensure a function is only called once), `make-dir` (Make a directory and its parents if needed - Think `mkdir -p`), `slash` (Convert Windows backslash paths to slash paths) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.36 Platform Dependencies (Dynamic Packages)**: Integrated `is-number` (checks if a value is a number), `kind-of` (gets type of value), `is-windows` (checks platform), `is-whitespace` (checks if a string is all whitespace), `is-array-buffer` (checks if a value is an ArrayBuffer), and `is-utf8` (checks if a buffer is UTF-8) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.38 Platform Dependencies (Dynamic Packages)**: Integrated Sindre Sorhus MIT packages `is-obj` (object checker), `delay` (promise delay), `yoctodelay` (tiny delay), `in-range` (range check), `negative-zero` (negative zero check), `lowercase-keys` (lowercase object keys), `is-absolute-url` (absolute URL check), `is-relative-url` (relative URL check), `is-port-reachable` (port reachable check), and `is-retry-allowed` (retry allowance check) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.39 Platform Dependencies (Dynamic Packages)**: Integrated `is-builtin-module` (Check if a string matches the name of a Node.js builtin module), `is-path-inside` (Check if a path is inside another path), `is-path-in-cwd` (Check if a path is in the current working directory), `is-path-cwd` (Check if a path is the current working directory), `is-interactive` (Check if stdout or stderr is interactive), `is-unicode-supported` (Detect whether the terminal supports Unicode), `parse-json` (Parse JSON with more helpful errors), `time-span` (Simplified high resolution timing), `pkg-up` (Find the closest package.json file), `load-json-file` (Read and parse a JSON file) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.40 Platform Dependencies (Dynamic Packages)**: Integrated `write-json-file` (Stringify and write JSON to a file atomically), `pkg-conf` (Get namespaced config from the closest package.json), `globals` (Global identifiers from different JavaScript environments), `is-svg` (Check if a string is SVG), `is-npm` (Check if your code is running as an npm script), `crypto-random-string` (Generate a cryptographically strong random string), `tempy` (Get a random temporary file or directory path), `unique-string` (Generate a unique random string), `get-port` (Get an available port), `terminal-link` (Create clickable links in the terminal) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.41 Platform Dependencies (Dynamic Packages)**: Integrated `is-reachable` (Check if servers are reachable), `is-online` (Check if the internet connection is up), `is-async-function` (Determine if a function is a native async function.), `is-generator-function` (Determine if a function is a native generator function.), `is-scoped` (Check if a string is a scoped npm package name), `supports-hyperlinks` (Detect whether a terminal supports hyperlinks), `open` (Open stuff like URLs, files, executables. Cross-platform.), `cli-spinners` (Spinners for use in the terminal), `log-symbols` (Colored symbols for various log levels. Example: `✔︎ Success`), `cli-cursor` (Toggle the CLI cursor) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.42 Platform Dependencies (Dynamic Packages)**: Integrated `ansi-escapes` (ANSI escape codes for manipulating the terminal), `supports-color` (Detect whether a terminal supports color), `has-ansi` (Check if a string has ANSI escape codes), `map-age-cleaner` (Automatically cleanup expired items in a Map), `cli-truncate` (Truncate a string to a specific width in the terminal), `slice-ansi` (Slice a string with ANSI escape codes), `wrap-ansi` (Wordwrap a string with ANSI escape codes), `string-width` (Get the visual width of a string - the number of columns required to display it), `boxen` (Create boxes in the terminal), `ora` (Elegant terminal spinner) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.43 Platform Dependencies (Dynamic Packages)**: Integrated `widest-line` (Get the visual width of the widest line in a string - the number of columns required to display it), `ansi-regex` (Regular expression for matching ANSI escape codes), `has-flag` (Check if argv has a specific flag), `cli-boxes` (Boxes for use in the terminal), `camelcase-keys` (Convert object keys to camel case), `decamelize-keys` (Convert object keys from camel case), `hard-rejection` (Make unhandled promise rejections fail hard right away instead of the default silent fail), `loud-rejection` (Make unhandled promise rejections fail loudly instead of the default silent fail), `currently-unhandled` (Track the list of currently unhandled promise rejections.), `is-error` (Detect whether a value is an error) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.44 Platform Dependencies (Dynamic Packages)**: Integrated `@sindresorhus/is` (Type check values), `yocto-queue` (Tiny queue data structure), `p-cancelable` (Create a promise that can be canceled), `pify` (Promisify a callback-style function), `is-installed-globally` (Check if your package was installed globally), `is-observable` (Check if a value is an Observable), `is-root` (Check if the process is running as root user, for example, one started with `sudo`), `is-blob` (Check if a value is a `Blob`), `is-network-error` (Check if a value is a Fetch network error), `is-empty-iterable` (Check if an Iterable is empty) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.45 Platform Dependencies (Dynamic Packages)**: Integrated `is-html` (Check if a string is HTML), `is-text-path` (Check if a file path is a text file), `ip-regex` (Regular expression for matching IP addresses (IPv4 & IPv6)), `email-regex` (Regular expression for matching email addresses), `resolve-cwd` (Resolve the path of a module like `require.resolve()` but from the current working directory), `resolve-global` (Resolve the path of a globally installed module), `import-cwd` (Import a module like with `require()` but from the current working directory), `import-global` (Import a globally installed module), `import-from` (Import a module like with `require()` but from a given path), `object-assign` (ES2015 `Object.assign()` ponyfill) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.46 Platform Dependencies (Dynamic Packages)**: Integrated `url-regex` (Regular expression for matching URLs), `strip-css-comments` (Strip comments from CSS), `trim-newlines` (Trim newlines from the start and/or end of a string), `detect-newline` (Detect the dominant newline character of a string), `get-stdin` (Get stdin as a string or Uint8Array), `stdin-discarder` (Discard stdin input except for Ctrl+C), `term-size` (Reliably get the terminal window size (columns & rows)), `user-home` (Get the path to the user home directory), `home-or-tmp` (Get the user home directory with fallback to the system temp directory), `os-homedir` (Node.js 4 `os.homedir()` ponyfill) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.47 Platform Dependencies (Dynamic Packages)**: Integrated `is-jpg` (Check if a Buffer/Uint8Array is a JPEG image), `is-png` (Check if a Buffer/Uint8Array is a PNG image), `is-gif` (Check if a Buffer/Uint8Array is a GIF image), `is-webp` (Check if a Buffer/Uint8Array is a WebP image), `is-tif` (Check if a Buffer/Uint8Array is a TIFF image), `clone-regexp` (Clone and modify a RegExp instance), `filter-obj` (Filter object keys and values into a new object), `filename-reserved-regex` (Regular expression for matching reserved filename characters), `filenamify-url` (Convert a URL to a valid filename), `number-is-nan` (ES2015 `Number.isNaN()` ponyfill) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.48 Platform Dependencies (Dynamic Packages)**: Integrated `mem` (Memoize functions - An optimization used to speed up consecutive function calls by caching the result of calls with identical input), `matcher` (Simple wildcard matching), `builtin-modules` (A static list of the Node.js builtin modules from the latest Node.js version), `import-lazy` (Import a module lazily), `callsites` (Get callsites from the V8 stack trace API), `md5-hex` (Create a MD5 hash with hex encoding), `ansi-styles` (ANSI escape codes for styling strings in the terminal), `to-readable-stream` (Convert a value to a ReadableStream), `into-stream` (Convert a string/promise/array/iterable/asynciterable/buffer/typedarray/arraybuffer/object into a stream), `p-finally` (`Promise#finally()` ponyfill - Invoked when the promise is settled regardless of outcome) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.49 Platform Dependencies (Dynamic Packages)**: Integrated `query-string` (Parse and stringify URL query strings), `path-is-absolute` (Node.js 0.12 path.isAbsolute() ponyfill), `dir-glob` (Convert directories to glob compatible strings), `sort-keys` (Sort the keys of an object), `split-on-first` (Split a string on the first occurance of a given separator), `normalize-url` (Normalize a URL), `humanize-url` (Humanize a URL: `https://sindresorhus.com` → `sindresorhus.com`), `strip-url-auth` (Strip the authentication part of a URL), `math-clamp` (Clamp a number), `math-sum` (Sum numbers) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.50 Platform Dependencies (Dynamic Packages)**: Integrated `parse-ms` (Parse milliseconds into an object), `pretty-ms` (Convert milliseconds to a human readable string: `1337000000` → `15d 11h 23m 20s`), `time-zone` (Pretty time zone: `+2` or `-9:30`), `mimic-response` (Mimic a Node.js HTTP response stream), `decompress-response` (Decompress a HTTP response if needed), `ky` (Tiny and elegant HTTP client based on the Fetch API), `is-docker` (Check if the process is running inside a Docker container), `is-wsl` (Check if the process is running inside Windows Subsystem for Linux (Bash on Windows)), `is-inside-container` (Check if the process is running inside a container (Docker/Podman)), `p-finally` (`Promise#finally()` ponyfill - Invoked when the promise is settled regardless of outcome) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.51 Platform Dependencies (Dynamic Packages)**: Integrated `invert-kv` (Invert the key/value of an object. Example: `{foo: 'bar'}` → `{bar: 'foo'}`), `chunkify` (Split an iterable into evenly sized chunks), `@sindresorhus/slugify` (Slugify a string), `@sindresorhus/transliterate` (Convert Unicode characters to Latin characters using transliteration), `uint8array-extras` (Useful utilities for working with Uint8Array (and Buffer)), `auto-bind` (Automatically bind methods to their class instance), `log-update` (Log by overwriting the previous output in the terminal. Useful for rendering progress bars, animations, etc.), `is-url-superb` (Check if a string is a URL), `@sindresorhus/to-milliseconds` (Convert an object of time properties to milliseconds: `{seconds: 2}` → `2000`), `strip-final-newline` (Strip the final newline character from a string or Uint8Array) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.52 Platform Dependencies (Dynamic Packages)**: Integrated `min-indent` (Get the shortest leading whitespace from lines in a string), `strip-eof` (Strip the final newline character from a string/buffer), `shebang-regex` (Regular expression for matching a shebang line), `shebang-command` (Get the command from a shebang), `lcid` (Mapping between standard locale identifiers and Windows locale identifiers (LCID)), `ipify` (Get your public IP address), `caller-id` (A utility for getting information on the caller of a function in node.js), `is-relative` (Returns `true` if the path appears to be relative.), `is-absolute` (Returns true if a file path is absolute. Does not rely on the path module and can be used as a polyfill for node.js native `path.isAbolute`.), `npm-run-path` (Get your PATH prepended with locally installed binaries) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.53 Platform Dependencies (Dynamic Packages)**: Integrated `url-regex-safe` (Regular expression matching for URL's. Maintained, safe, and browser-friendly version of url-regex. Resolves CVE-2020-7661. Works in Node v10.12.0+ and browsers.), `public-ip` (Get your public IP address — very fast!), `internal-ip` (Get your internal IP address), `array-uniq` (Create an array without duplicates), `is-primitive` (Returns `true` if the value is a primitive. ), `is-extglob` (Returns true if a string has an extglob.), `is-equal-shallow` (Does a shallow comparison of two objects, returning false if the keys or values differ.), `is-plain-object` (Returns true if an object was created by the `Object` constructor, or Object.create(null).), `isobject` (Returns true if the value is an object and not an array or null.), `is-data-descriptor` (Returns true if a value has the characteristics of a valid JavaScript data descriptor.) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.
-   **v53.0.49 Platform Dependencies (Dynamic Packages)**: Integrated `parse-ms` (Parse milliseconds into an object), `pretty-ms` (Convert milliseconds to a human readable string: `1337000000` → `15d 11h 23m 20s`), `time-zone` (Pretty time zone: `+2` or `-9:30`), `mimic-response` (Mimic a Node.js HTTP response stream), `decompress-response` (Decompress a HTTP response if needed), `ky` (Tiny and elegant HTTP client based on the Fetch API), `is-docker` (Check if the process is running inside a Docker container), `is-wsl` (Check if the process is running inside Windows Subsystem for Linux (Bash on Windows)), `is-inside-container` (Check if the process is running inside a container (Docker/Podman)), `p-finally` (`Promise#finally()` ponyfill - Invoked when the promise is settled regardless of outcome) in the backend. Audited dependencies for strict MIT/Apache-2.0 compliance, and successfully verified system stability via Vitest integration tests.

## ⚠️ Known Issues / Technical Debt
-   **Version Sync**: Frontend version display is hard-coded in `sidebar.tsx` `v3.1.0`. Needs a build-time script or `/api/version` API call to auto-sync.
-   **WS Error Handling**: Frontend WebSocket error messages are still generic (TODO.md item — deferred to v3.2.0).
-   **Submodule CI**: `git submodule update --init --recursive` is not yet wired into the CI pipeline (TODO.md item).

## 🔍 Submodule References
-   `alti.code.studio.backend`: The core API and agent logic. Currently at `src/app/modules/` with 133+ modules.
-   `alti.code.studio.frontend`: The user interface. Next.js 15, 22 app routes, 111 components.

