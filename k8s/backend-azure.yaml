apiVersion: apps/v1
kind: Deployment
metadata:
  name: alti-backend-azure
  namespace: default
  labels:
    app: alti-backend-azure
spec:
  replicas: 2
  selector:
    matchLabels:
      app: alti-backend-azure
  template:
    metadata:
      labels:
        app: alti-backend-azure
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: sovereign.cloud/environment
                operator: In
                values:
                - azure
      tolerations:
      - key: "sovereign.cloud/environment"
        operator: "Equal"
        value: "azure"
        effect: "NoSchedule"
      containers:
      - name: backend-azure
        image: registry.internal.libertycenterone.com/alti-backend-azure:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5002
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "5002"
        securityContext:
          drop: ["ALL"]
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
        resources:
          requests:
            cpu: "500m"
            memory: "1Gi"
          limits:
            cpu: "2000m"
            memory: "4Gi"
        readinessProbe:
          httpGet:
            path: /healthz
            port: 5002
          initialDelaySeconds: 15
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /healthz
            port: 5002
          initialDelaySeconds: 30
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: alti-backend-azure
spec:
  type: ClusterIP
  selector:
    app: alti-backend-azure
  ports:
  - port: 80
    targetPort: 5002
    protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: alti-backend-azure-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: alti-backend-azure
  minReplicas: 2
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: alti-backend-azure-policy
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: alti-backend-azure
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
  egress:
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0 # Restricted to Azure OpenAI IP ranges in production
    ports:
    - protocol: TCP
      port: 443
